package sandbox

// Foo returns a friendly greeting.
func Foo(name string) string {
	return "Hello, " + name + "!"
}

// Bar adds two ints. It exists only to keep the example short ☺.
func Bar(a, b int) int {
	return a + b
}
